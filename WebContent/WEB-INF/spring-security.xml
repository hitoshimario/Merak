<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
    					http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                        http://www.springframework.org/schema/security
                        http://www.springframework.org/schema/security/spring-security-3.0.xsd">
	
	<!-- <global-method-security secured-annotations="enabled"/>  -->
	
	<http auto-config="true">
		<intercept-url pattern="/master/**" access="ROLE_ADMIN" />
		<form-login login-page="/login" 
			default-target-url="/master/home" 
			authentication-failure-url="/login" />
		<logout logout-success-url="/login"/>
		
		<session-management>
        	<concurrency-control max-sessions="1"/>
        </session-management>
	</http>
 	
 	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource" 
			
				users-by-username-query="
					select username, password, enabled 
		      		from t_user where username=?" 
		      
				authorities-by-username-query="
		      		select u.username, r.authority from t_user u
		      		left join t_user_role ur on u.id = ur.id_user
		      		left join t_role r on r.id = ur.id_role
		      		where u.username=?" 
		      
			/>
		</authentication-provider>
	</authentication-manager>
	
</beans:beans>